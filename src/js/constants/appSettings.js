/**
 *  React Appsetting Contants
 */

const AppSettings = {
  route : '/',

  apiPath : "http://localhost",
  websocketPath : 'ws://localhost',

  componentMessages : {
    progressed : null,
    completed : null,
    failed : null
  }
};

module.exports = AppSettings;